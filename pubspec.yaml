name: english_family_app
description: English Family App
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  record: ^6.0.0
  audioplayers: ^6.0.0
  path_provider: ^2.1.3

flutter:
  uses-material-design: true
